SELECT *
FROM users
ORDER BY age;

SELECT *
FROM users
ORDER BY age ASC;

SELECT *
FROM users
ORDER BY age DESC;

SELECT *
FROM users
ORDER BY name;

SELECT *
FROM users
WHERE age > 23
ORDER BY age;

SELECT DISTINCT age
FROM users;

SELECT DISTINCT age, name
FROM users;

SELECT DISTINCT age
FROM users
ORDER BY age;